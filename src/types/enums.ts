export enum UserGameStatus {
  SUCCESS = "Success",
  TOO_SOON = "Too Soon",
  WRONG_KEY = "Wrong Key",
  TOO_LATE = "Too late",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  UNDETERMINED = "undetermined",
}
